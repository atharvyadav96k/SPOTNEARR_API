name: Multiple Go Functions Deploy

on:
  push:
    branches:
      - main
    paths:
      # ----------- Auth Functions ---------
      - 'auth/business/register/**'
      - 'auth/business/login/**'
      - 'auth/user/login/**'
      - 'auth/user/register/**'

      # ----------- Bridge Service Functions ---------
      - 'bridge-service/filter-follow/**'
      - 'bridge-service/filter-likes/**'
      - 'bridge-service/push_notification/**'

      # ----------- Business Functions ---------
      - 'business/categories/add_categories/**'
      - 'business/categories/get_categories/**'

      - 'business/plans/add_plans/**'
      - 'business/plans/get_active_plan/**'
      - 'business/plans/get_business_plans/**'
      - 'business/plans/set_business_active_plan/**'

      - 'business/spotlights/create_spotlight/**'
      - 'business/spotlights/get_spotlights/**'
      - 'business/spotlights/update_spotlight/**'
      - 'business/spotlights/delete_spotlight/**'

      - 'business/products/new_products/**'
      - 'business/products/get_products/**'
      - 'business/products/update_products/**'
      - 'business/products/delete_products/**'
      - 'business/products/get_business_products/** '


      # ---------- User Functions ---------
      - 'users/follows/follow-business/**'
      - 'users/follows/unfollow-business/**'
      - 'users/follows/get-following-businesses/**'
      
      - 'users/likes/business-like/**'
      - 'users/likes/business-dislike/**'


      # --------- Spotlight Functions ---------
      - 'users/spotlights/followed_business_spotlights/**'
      - 'users/spotlights/get_liked_spotlights/**'
      - 'users/spotlights/get_spotlight_by_location/**'

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      # ---------- Auth Functions ---------
      business-register: ${{ steps.filter.outputs.business-register }}
      business-login: ${{ steps.filter.outputs.business-login }}
      user-login: ${{ steps.filter.outputs.user-login }}
      user-register: ${{ steps.filter.outputs.user-register }}

      # --------- Bridge Service Functions ---------
      bridge-service-push-notification: ${{ steps.filter.outputs.bridge-service-push-notification }}
      
      # --------- Business Functions ---------
      business-categories-add: ${{ steps.filter.outputs.business-categories-add }}
      business-categories-get: ${{ steps.filter.outputs.business-categories-get }}

      business-plans-add: ${{ steps.filter.outputs.business-plans-add }}
      business-plans-get-active: ${{ steps.filter.outputs.business-plans-get-active }}
      business-plans-get-business: ${{ steps.filter.outputs.business-plans-get-business }}
      business-plans-set-active: ${{ steps.filter.outputs.business-plans-set-active }}

      business-spotlights-create: ${{ steps.filter.outputs.business-spotlights-create }}
      business-spotlights-get: ${{ steps.filter.outputs.business-spotlights-get }}
      business-spotlights-update: ${{ steps.filter.outputs.business-spotlights-update }}
      business-spotlights-delete: ${{ steps.filter.outputs.business-spotlights-delete }}  

      # --------- User Functions ---------
      users-follow: ${{ steps.filter.outputs.users-follow }}
      users-unfollow: ${{ steps.filter.outputs.users-unfollow }}
      users-get-following-businesses: ${{ steps.filter.outputs.users-get-following-businesses }}

      users-business-like: ${{ steps.filter.outputs.users-business-like }}
      users-business-dislike: ${{ steps.filter.outputs.users-business-dislike }}

      # ---------- Spotlight Functions ---------
      users-followed-business-spotlights: ${{ steps.filter.outputs.users-followed-business-spotlights }}
      users-get-liked-spotlights: ${{ steps.filter.outputs.users-get-liked-spotlights }}
      users-get-spotlight-by-location: ${{ steps.filter.outputs.users-get-spotlight-by-location }}
      
    steps:
      - uses: actions/checkout@v4

      - id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
          
            business-register:
              - 'auth/business/register/**'
            business-login:
              - 'auth/business/login/**'
            user-login:
              - 'auth/user/login/**'      
            user-register:
              - 'auth/user/register/**'  


            bridge-service-push-notification:
              - 'bridge-service/push_notification/**'


            business-categories-add:
              - 'business/categories/add_categories/**'
            business-categories-get:
              - 'business/categories/get_categories/**'

            business-plans-add:
              - 'business/plans/add_plans/**'
            business-plans-get-active:
              - 'business/plans/get_active_plan/**'
            business-plans-get-business:
              - 'business/plans/get_business_plans/**'
            business-plans-set-active:
              - 'business/plans/set_business_active_plan/**'

            business-spotlights-create:
              - 'business/spotlights/create_spotlight/**'
            business-spotlights-get:
              - 'business/spotlights/get_spotlights/**'
            business-spotlights-update:
              - 'business/spotlights/update_spotlight/**'
            business-spotlights-delete:
              - 'business/spotlights/delete_spotlight/**'

            users-follow:
              - 'users/follows/follow-business/**'
            users-unfollow:
              - 'users/follows/unfollow-business/**'
            users-get-following-businesses:
              - 'users/follows/get-following-businesses/**'

            users-business-like:
              - 'users/likes/business-like/**'
            users-business-dislike:
              - 'users/likes/business-dislike/**'

            users-followed-business-spotlights:
              - 'users/spotlights/followed_business_spotlights/**'
            users-get-liked-spotlights:
              - 'users/spotlights/get_liked_spotlights/**'
            users-get-spotlight-by-location:
              - 'users/spotlights/get_spotlight_by_location/**'

  business-register:
    needs: detect-changes
    if: needs.detect-changes.outputs.business-register == 'true'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-function.yml
    with:

      working-directory: auth/business/register
      gcp_region: ${{ vars.GCP_REGION }}
    secrets: inherit

  business-login:
    needs: detect-changes
    if: needs.detect-changes.outputs.business-login == 'true'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-function.yml
    with:
      working-directory: auth/business/login
      gcp_region: ${{ vars.GCP_REGION }}
    secrets: inherit

# --------------------- Auth ------------------
  user-login:
    needs: detect-changes
    if: needs.detect-changes.outputs.user-login == 'true'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-function.yml
    with:
      working-directory: auth/user/login
      gcp_region: ${{ vars.GCP_REGION }}
    secrets: inherit

  user-register:
    needs: detect-changes
    if: needs.detect-changes.outputs.user-register == 'true'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-function.yml
    with:
      working-directory: auth/user/register
      gcp_region: ${{ vars.GCP_REGION }}
    secrets: inherit

# --------------------- Bridge Service ------------------
  bridge-service-push-notification:
    needs: detect-changes
    if: needs.detect-changes.outputs.bridge-service-push-notification == 'true'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-function.yml
    with:
      function-name: push_notification
      working-directory: bridge-service/push_notification
      gcp_region: ${{ vars.GCP_REGION }}
      tf_vars: >-
        -var="topic=${{ vars.GCP_TOPIC_PUSH_NOTIFICATION }}"
    secrets: inherit


# --------------------- Business ------------------
  business-categories-add:
    needs: detect-changes
    if: needs.detect-changes.outputs.business-categories-add == 'true'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-function.yml
    with:
      function-name: add_categories
      working-directory: business/categories/add_categories
      gcp_region: ${{ vars.GCP_REGION }}
    secrets: inherit

  business-categories-get:
    needs: detect-changes
    if: needs.detect-changes.outputs.business-categories-get == 'true'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-function.yml
    with:
      function-name: get_categories
      working-directory: business/categories/get_categories
      gcp_region: ${{ vars.GCP_REGION }}
    secrets: inherit
  
  business-plans-add:
    needs: detect-changes
    if: needs.detect-changes.outputs.business-plans-add == 'true'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-function.yml
    with:
      function-name: add_plans
      working-directory: business/plans/add_plans
      gcp_region: ${{ vars.GCP_REGION }}
    secrets: inherit
  
  business-plans-get-active:
    needs: detect-changes
    if: needs.detect-changes.outputs.business-plans-get-active == 'true'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-function.yml
    with:
      function-name: get_active_plan
      working-directory: business/plans/get_active_plan
      gcp_region: ${{ vars.GCP_REGION }}
    secrets: inherit

  business-plans-get-business:
    needs: detect-changes
    if: needs.detect-changes.outputs.business-plans-get-business == 'true'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-function.yml
    with:
      function-name: get_business_plans
      working-directory: business/plans/get_business_plans
      gcp_region: ${{ vars.GCP_REGION }}
    secrets: inherit

  business-plans-set-active:
    needs: detect-changes
    if: needs.detect-changes.outputs.business-plans-set-active == 'true'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-function.yml
    with:
      function-name: set_business_active_plan
      working-directory: business/plans/set_business_active_plan
      gcp_region: ${{ vars.GCP_REGION }}
    secrets: inherit

  business-spotlights-create:
    needs: detect-changes
    if: needs.detect-changes.outputs.business-spotlights-create == 'true'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-function.yml
    with:
      function-name: create_spotlight
      working-directory: business/spotlights/create_spotlight
      gcp_region: ${{ vars.GCP_REGION }}
    secrets: inherit
  
  business-spotlights-get:
    needs: detect-changes
    if: needs.detect-changes.outputs.business-spotlights-get == 'true'
    permissions:
      id-token: write
      contents: read
    
    uses: ./.github/workflows/deploy-function.yml
    with:
      function-name: get_spotlights
      working-directory: business/spotlights/get_spotlights
      gcp_region: ${{ vars.GCP_REGION }}
    secrets: inherit

  business-spotlights-update:
    needs: detect-changes
    if: needs.detect-changes.outputs.business-spotlights-update == 'true'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-function.yml
    with:
      function-name: update_spotlight
      working-directory: business/spotlights/update_spotlight
      gcp_region: ${{ vars.GCP_REGION }}
    secrets: inherit

  business-spotlights-delete:
    needs: detect-changes
    if: needs.detect-changes.outputs.business-spotlights-delete == 'true'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-function.yml
    with:
      function-name: delete_spotlight
      working-directory: business/spotlights/delete_spotlight
      gcp_region: ${{ vars.GCP_REGION }}
    secrets: inherit
   
  

# --------------------- User ------------------

  user-follow:
    needs: detect-changes
    if: needs.detect-changes.outputs.users-follow == 'true'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-function.yml
    with:
      function-name: user-follow
      working-directory: users/follows/follow-business
      gcp_region: ${{ vars.GCP_REGION }}
    secrets: inherit

  user-unfollow:
    needs: detect-changes
    if: needs.detect-changes.outputs.users-unfollow == 'true'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-function.yml
    with:
      function-name: user-unfollow
      working-directory: users/follows/unfollow-business
      gcp_region: ${{ vars.GCP_REGION }}
    secrets: inherit
  
  user-get-following-businesses:
    needs: detect-changes
    if: needs.detect-changes.outputs.users-get-following-businesses == 'true'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-function.yml
    with:
      function-name: user-get-following-businesses
      working-directory: users/follows/get-following-businesses
      gcp_region: ${{ vars.GCP_REGION }}
    secrets: inherit
  user-business-like:
    needs: detect-changes
    if: needs.detect-changes.outputs.users-business-like == 'true'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-function.yml
    with:
      function-name: user-like
      working-directory: users/likes/business-like
      gcp_region: ${{ vars.GCP_REGION }}
    secrets: inherit

  user-business-dislike:
    needs: detect-changes
    if: needs.detect-changes.outputs.users-business-dislike == 'true'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-function.yml
    with:
      function-name: user-dislike
      working-directory: users/likes/business-dislike
      gcp_region: ${{ vars.GCP_REGION }}
    secrets: inherit

# --------------------- Spotlight ------------------

  users-followed-business-spotlights:
    needs: detect-changes
    if: needs.detect-changes.outputs.users-followed-business-spotlights == 'true'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-function.yml
    with:
      function-name: get_followed_spotlights
      working-directory: users/spotlights/followed_business_spotlights
      gcp_region: ${{ vars.GCP_REGION }}
    secrets: inherit

  users-get-liked-spotlights:
    needs: detect-changes
    if: needs.detect-changes.outputs.users-get-liked-spotlights == 'true'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-function.yml
    with:
      function-name: get_liked_spotlights
      working-directory: users/spotlights/get_liked_spotlights
      gcp_region: ${{ vars.GCP_REGION }}
    secrets: inherit

  users-get-spotlight-by-location:
    needs: detect-changes
    if: needs.detect-changes.outputs.users-get-spotlight-by-location == 'true'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-function.yml
    with:
      function-name: get_spotlight_by_location
      working-directory: users/spotlights/get_spotlight_by_location
      gcp_region: ${{ vars.GCP_REGION }}
    secrets: inherit